---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.19.0
  name: downloads.download.beebs.dev
spec:
  group: download.beebs.dev
  names:
    kind: Download
    listKind: DownloadList
    plural: downloads
    singular: download
  scope: Namespaced
  versions:
  - name: v1
    schema:
      openAPIV3Schema:
        description: Download is the Schema for the downloads API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: spec defines the desired state of Download
            properties:
              formatSelection:
                description: |-
                  FormatSelection determines the video/audio quality and codec options.
                  This maps directly to yt-dlp's --format/-f flag syntax.
                  Example: "bestvideo[height<=1080]+bestaudio/best[height<=1080]"
                type: string
              inputURL:
                description: InputURL is the source URL for yt-dlp to download (e.g.,
                  a YouTube or Vimeo link).
                type: string
              output:
                description: OutputTarget specifies where the final file should be
                  stored.
                properties:
                  s3:
                    description: S3 specifies the S3 bucket destination.
                    properties:
                      bucket:
                        description: Bucket is the name of the S3 bucket.
                        type: string
                      key:
                        description: Key is the full path/filename within the bucket
                          (e.g., "videos/myvideo.mp4").
                        type: string
                      secretRef:
                        description: |-
                          SecretRef refers to a Kubernetes Secret containing credentials for the S3 target.
                          The Secret must contain keys like 'accessKeyID' and 'secretAccessKey'.
                        type: string
                    required:
                    - bucket
                    - key
                    - secretRef
                    type: object
                type: object
              postProcessing:
                description: PostProcessing contains options for post-download operations
                  like conversion.
                properties:
                  audioFormat:
                    description: |-
                      AudioFormat specifies the output format for audio extraction (e.g., "mp3", "flac").
                      Corresponds to yt-dlp '--audio-format'.
                    type: string
                  extractAudio:
                    description: |-
                      ExtractAudio converts the file to audio-only (e.g., mp3 or opus).
                      Corresponds to yt-dlp '--extract-audio'.
                    type: boolean
                type: object
            required:
            - inputURL
            - output
            type: object
          status:
            description: status defines the observed state of Download
            properties:
              completionTime:
                description: CompletionTime records when the download finished.
                format: date-time
                type: string
              conditions:
                description: conditions represent the current state of the Download
                  resource.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              downloadRate:
                description: |-
                  DownloadRate reports the last known download speed (e.g., "10.3 MiB/s").
                  Maps to bytes_per_second in the pseudo-manifest.
                type: string
              errorMessage:
                description: ErrorMessage is set if the Phase is 'Failed'.
                type: string
              jobName:
                description: |-
                  JobName is the name of the Kubernetes Job/Pod running the download.
                  This is useful for debugging and tracking the underlying resource.
                type: string
              phase:
                description: |-
                  Phase represents the current state of the download job
                  (e.g., Pending, Running, Completed, Failed).
                  This tracks the overall lifecycle of the Download resource.
                type: string
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
